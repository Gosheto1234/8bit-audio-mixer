name: Build and Publish EXE

# Grant the token permission to create/releases and upload assets
permissions:
  contents: write

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: windows-latest

    steps:
      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: 🐍 Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: 📦 Install dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          if (Test-Path requirements.txt) { pip install -r requirements.txt }
          pip install pyinstaller

      - name: 🔨 Build EXE with PyInstaller
        run: pyinstaller --onefile --noconsole --icon=icon.ico audiomix.py

      - name: 📤 Upload EXE as Build Artifact
        uses: actions/upload-artifact@v4
        with:
          name: audio-mixer-win32
          path: dist/audiomix.exe

      - name: 🏷️ Create GitHub Release & Upload EXE
        uses: ncipollo/release-action@v1
        with:
          tag: v${{ github.run_number }}            # `v1`, `v2`, ...
          release-name: Release ${{ github.run_number }}
          assets: dist/audiomix.exe
          overwrite: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
